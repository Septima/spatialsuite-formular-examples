<?xml version="1.0" encoding="UTF-8"?>
<datasources>
    <endpoint endpointtype="postgis" name="mailsender">
        <connect>[module.formular-config.connection]</connect>
        <user>[module.formular-config.user]</user>
        <pwd>[module.formular-config.pwd]</pwd>
        <srid>25832</srid>
        <table geometrycolumn="the_geom" pkcolumn="ogc_fid"/>
    </endpoint>
    <datasource displayname="Ventilationsanlæg" endpoint="selvbetjening" name="ds_formular_ventilation">
        <sql command="write">insert into formular_ventilation
		(
		wkb_geometry, 
		dato, 
		formular, 
		ref,
		stednavn,
		adresse, 
		aggregatplacering, 
		betjeningsomraade, 
		kontaktperson_navn, 
		kontaktperson_tlf, 
		sagsnr, 
		anlaeg_nr, 
		rev_dato, 
		rev_navn, 
		tegning_nr, 
		ventilationprincip, 
		vent_type,
		vent_automatik,
		vent_luftmaengde_proj, 
		vent_luftmaengde_maalt, 
		vent_statisk_tryk_proj, 
		vent_statisk_tryk_maalt, 
		vent_total_tryk_proj, 
		vent_total_tryk_maalt, 
		vent_omdrejning_proj, 
		vent_omdrejning_maalt, 
		vent_maks_omdr_proj, 
		vent_maks_omdr_maalt, 
		vent_sef_proj, 
		vent_sef_maalt, 
		motor_type, 
		motor_effekt_proj, 
		motor_effekt_maalt, 
		motor_spaending_proj, 
		motor_spaending_maalt, 
		motor_stroemforbrug_proj, 
		motor_stroemforbrug_maalt, 
		motor_omdrejning_proj, 
		motor_omdrejning_maalt, 
		motor_cos_proj, 
		motor_cos_maalt, 
		drivkraft, 
		rem_ventilator_remskive, 
		rem_ventilator_udbor, 
		rem_motor_remskive, 
		rem_motor_udbor, 
		rem_kileremme_antal, 
		rem_kileremme_type, 
		rem_centerafstand, 
		rem_justering,
		ud_ventilationprincip, 
		ud_vent_type,
		ud_vent_automatik,
		ud_vent_luftmaengde_proj, 
		ud_vent_luftmaengde_maalt, 
		ud_vent_statisk_tryk_proj, 
		ud_vent_statisk_tryk_maalt, 
		ud_vent_total_tryk_proj, 
		ud_vent_total_tryk_maalt, 
		ud_vent_omdrejning_proj, 
		ud_vent_omdrejning_maalt, 
		ud_vent_maks_omdr_proj, 
		ud_vent_maks_omdr_maalt, 
		ud_vent_sef_proj, 
		ud_vent_sef_maalt, 
		ud_motor_type, 
		ud_motor_effekt_proj, 
		ud_motor_effekt_maalt, 
		ud_motor_spaending_proj, 
		ud_motor_spaending_maalt, 
		ud_motor_stroemforbrug_proj, 
		ud_motor_stroemforbrug_maalt, 
		ud_motor_omdrejning_proj, 
		ud_motor_omdrejning_maalt, 
		ud_motor_cos_proj, 
		ud_motor_cos_maalt, 
		ud_drivkraft, 
		ud_rem_ventilator_remskive, 
		ud_rem_ventilator_udbor, 
		ud_rem_motor_remskive, 
		ud_rem_motor_udbor, 
		ud_rem_kileremme_antal, 
		ud_rem_kileremme_type, 
		ud_rem_centerafstand, 
		ud_rem_justering,
		filter1_type, 
		filter1_antal, 
		filter1_nr, 
		filter1_klasse, 
		filter1_bredde, 
		filter1_hoejde, 
		filter1_laengde, 
		filter1_pose_antal, 
		filter2_type, 
		filter2_antal, 
		filter2_nr, 
		filter2_klasse, 
		filter2_bredde, 
		filter2_hoejde, 
		filter2_laengde, 
		filter2_pose_antal, 
		filter3_type, 
		filter3_antal, 
		filter3_nr, 
		filter3_klasse, 
		filter3_bredde, 
		filter3_hoejde, 
		filter3_laengde, 
		filter3_pose_antal, 
		bemaerkning, 
		filter1_ud_type, 
		filter1_ud_antal, 
		filter1_ud_nr, 
		filter1_ud_klasse, 
		filter1_ud_bredde, 
		filter1_ud_hoejde, 
		filter1_ud_laengde, 
		filter1_ud_pose_antal, 
		filter2_ud_type, 
		filter2_ud_antal, 
		filter2_ud_nr, 
		filter2_ud_klasse, 
		filter2_ud_bredde, 
		filter2_ud_hoejde, 
		filter2_ud_laengde, 
		filter2_ud_pose_antal, 
		filter3_ud_type, 
		filter3_ud_antal, 
		filter3_ud_nr, 
		filter3_ud_klasse, 
		filter3_ud_bredde, 
		filter3_ud_hoejde, 
		filter3_ud_laengde, 
		filter3_ud_pose_antal, 
		bemaerkning_ud, 
		roegspjaeld_antal, 
		roegspjaeld_foto, 
		foto1, 
		foto2, 
		foto3, 
		foto4
		) 
			values 
			(
			[geometry: ToGeometry(wkt)],
			localtimestamp,
			[string: formular],
			[string: sessionid],
			[string: if isDefined(stednavn) then stednavn else "" endif],
			  [string: if isDefined(address) then address else "" endif],
			  [string: if isDefined(aggregatplacering) then aggregatplacering else "" endif],
			  [string: if isDefined(betjeningsomraade) then betjeningsomraade else "" endif],
			  [string: if isDefined(kontaktperson_navn) then kontaktperson_navn else "" endif],
			  [string: if isDefined(kontaktperson_tlf) then kontaktperson_tlf else "" endif],
			  [string: if isDefined(sagsnr) then sagsnr else "" endif],
			  [string: if isDefined(anlaeg_nr) then anlaeg_nr else "" endif],
			  [string: if isDefined(rev_dato) then rev_dato else "" endif],
			  [string: if isDefined(rev_navn) then rev_navn else "" endif],
			  [string: if isDefined(tegning_nr) then tegning_nr else "" endif],
			  [string: if isDefined(ventilationprincip) then ventilationprincip else "" endif],
			  [string: if isDefined(vent_type) then vent_type else "" endif],
			  [string: if isDefined(vent_automatik) then vent_automatik else "" endif],
			  [number: if isDefined(vent_luftmaengde_proj) then ToNumber(vent_luftmaengde_proj) else ToNumber(0) endif],
			  [number: if isDefined(vent_luftmaengde_maalt) then ToNumber(vent_luftmaengde_maalt) else ToNumber(0) endif],
			  [number: if isDefined(vent_statisk_tryk_proj) then ToNumber(vent_statisk_tryk_proj) else ToNumber(0) endif],
			  [number: if isDefined(vent_statisk_tryk_maalt) then ToNumber(vent_statisk_tryk_maalt) else ToNumber(0) endif],
			  [number: if isDefined(vent_total_tryk_proj) then ToNumber(vent_total_tryk_proj) else ToNumber(0) endif],
			  [number: if isDefined(vent_total_tryk_maalt) then ToNumber(vent_total_tryk_maalt) else ToNumber(0) endif],
			  [number: if isDefined(vent_omdrejning_proj) then ToNumber(vent_omdrejning_proj) else ToNumber(0) endif],
			  [number: if isDefined(vent_omdrejning_maalt) then ToNumber(vent_omdrejning_maalt) else ToNumber(0) endif],
			  [number: if isDefined(vent_maks_omdr_proj) then ToNumber(vent_maks_omdr_proj) else ToNumber(0) endif],
			  [number: if isDefined(vent_maks_omdr_maalt) then ToNumber(vent_maks_omdr_maalt) else ToNumber(0) endif],
			  [number: if isDefined(vent_sef_proj) then ToNumber(vent_sef_proj) else ToNumber(0) endif],
			  [number: if isDefined(vent_sef_maalt) then ToNumber(vent_sef_maalt) else ToNumber(0) endif],
			  [string: if isDefined(motor_type) then motor_type else "" endif],
			  [number: if isDefined(motor_effekt_proj) then ToNumber(motor_effekt_proj) else ToNumber(0) endif],
			  [number: if isDefined(motor_effekt_maalt) then ToNumber(motor_effekt_maalt) else ToNumber(0) endif],
			  [number: if isDefined(motor_spaending_proj) then ToNumber(motor_spaending_proj) else ToNumber(0) endif],
			  [number: if isDefined(motor_spaending_maalt) then ToNumber(motor_spaending_maalt) else ToNumber(0) endif],
			  [number: if isDefined(motor_stroemforbrug_proj) then ToNumber(motor_stroemforbrug_proj) else ToNumber(0) endif],
			  [number: if isDefined(motor_stroemforbrug_maalt) then ToNumber(motor_stroemforbrug_maalt) else ToNumber(0) endif],
			  [number: if isDefined(motor_omdrejning_proj) then ToNumber(motor_omdrejning_proj) else ToNumber(0) endif],
			  [number: if isDefined(motor_omdrejning_maalt) then ToNumber(motor_omdrejning_maalt) else ToNumber(0) endif],
			  [number: if isDefined(motor_cos_proj) then ToNumber(motor_cos_proj) else ToNumber(0) endif],
			  [number: if isDefined(motor_cos_maalt) then ToNumber(motor_cos_maalt) else ToNumber(0) endif],
			  [string: if isDefined(drivkraft) then drivkraft else "" endif],
 			  [string: if isDefined(rem_ventilator_remskive) then rem_ventilator_remskive else "" endif],
			  [string: if isDefined(rem_ventilator_udbor) then rem_ventilator_udbor else "" endif],
			  [string: if isDefined(rem_motor_remskive) then rem_motor_remskive else "" endif],
			  [string: if isDefined(rem_motor_udbor) then rem_motor_udbor else "" endif],
			  [number: if isDefined(rem_kileremme_antal) then ToNumber(rem_kileremme_antal) else ToNumber(0) endif],
			  [string: if isDefined(rem_kileremme_type) then rem_kileremme_type else "" endif],
			  [number: if isDefined(rem_centerafstand) then ToNumber(rem_centerafstand) else ToNumber(0) endif],
			  [number: if isDefined(rem_justering) then ToNumber(rem_justering) else ToNumber(0) endif],
			  [string: if isDefined(ud_ventilationprincip) then ud_ventilationprincip else "" endif],
			  [string: if isDefined(ud_vent_type) then ud_vent_type else "" endif],
			  [string: if isDefined(ud_vent_automatik) then ud_vent_automatik else "" endif],
			  [number: if isDefined(ud_vent_luftmaengde_proj) then ToNumber(ud_vent_luftmaengde_proj) else ToNumber(0) endif],
			  [number: if isDefined(ud_vent_luftmaengde_maalt) then ToNumber(ud_vent_luftmaengde_maalt) else ToNumber(0) endif],
			  [number: if isDefined(ud_vent_statisk_tryk_proj) then ToNumber(ud_vent_statisk_tryk_proj) else ToNumber(0) endif],
			  [number: if isDefined(ud_vent_statisk_tryk_maalt) then ToNumber(ud_vent_statisk_tryk_maalt) else ToNumber(0) endif],
			  [number: if isDefined(ud_vent_total_tryk_proj) then ToNumber(ud_vent_total_tryk_proj) else ToNumber(0) endif],
			  [number: if isDefined(ud_vent_total_tryk_maalt) then ToNumber(ud_vent_total_tryk_maalt) else ToNumber(0) endif],
			  [number: if isDefined(ud_vent_omdrejning_proj) then ToNumber(ud_vent_omdrejning_proj) else ToNumber(0) endif],
			  [number: if isDefined(ud_vent_omdrejning_maalt) then ToNumber(ud_vent_omdrejning_maalt) else ToNumber(0) endif],
			  [number: if isDefined(ud_vent_maks_omdr_proj) then ToNumber(ud_vent_maks_omdr_proj) else ToNumber(0) endif],
			  [number: if isDefined(ud_vent_maks_omdr_maalt) then ToNumber(ud_vent_maks_omdr_maalt) else ToNumber(0) endif],
			  [number: if isDefined(ud_vent_sef_proj) then ToNumber(ud_vent_sef_proj) else ToNumber(0) endif],
			  [number: if isDefined(ud_vent_sef_maalt) then ToNumber(ud_vent_sef_maalt) else ToNumber(0) endif],
			  [string: if isDefined(ud_motor_type) then ud_motor_type else "" endif],
			  [number: if isDefined(ud_motor_effekt_proj) then ToNumber(ud_motor_effekt_proj) else ToNumber(0) endif],
			  [number: if isDefined(ud_motor_effekt_maalt) then ToNumber(ud_motor_effekt_maalt) else ToNumber(0) endif],
			  [number: if isDefined(ud_motor_spaending_proj) then ToNumber(ud_motor_spaending_proj) else ToNumber(0) endif],
			  [number: if isDefined(ud_motor_spaending_maalt) then ToNumber(ud_motor_spaending_maalt) else ToNumber(0) endif],
			  [number: if isDefined(ud_motor_stroemforbrug_proj) then ToNumber(ud_motor_stroemforbrug_proj) else ToNumber(0) endif],
			  [number: if isDefined(ud_motor_stroemforbrug_maalt) then ToNumber(ud_motor_stroemforbrug_maalt) else ToNumber(0) endif],
			  [number: if isDefined(ud_motor_omdrejning_proj) then ToNumber(ud_motor_omdrejning_proj) else ToNumber(0) endif],
			  [number: if isDefined(ud_motor_omdrejning_maalt) then ToNumber(ud_motor_omdrejning_maalt) else ToNumber(0) endif],
			  [number: if isDefined(ud_motor_cos_proj) then ToNumber(ud_motor_cos_proj) else ToNumber(0) endif],
			  [number: if isDefined(ud_motor_cos_maalt) then ToNumber(ud_motor_cos_maalt) else ToNumber(0) endif],
			  [string: if isDefined(ud_drivkraft) then ud_drivkraft else "" endif],
			  [string: if isDefined(ud_rem_ventilator_remskive) then ud_rem_ventilator_remskive else "" endif],
			  [string: if isDefined(ud_rem_ventilator_udbor) then ud_rem_ventilator_udbor else "" endif],
			  [string: if isDefined(ud_rem_motor_remskive) then ud_rem_motor_remskive else "" endif],
			  [string: if isDefined(ud_rem_motor_udbor) then ud_rem_motor_udbor else "" endif],
			  [number: if isDefined(ud_rem_kileremme_antal) then ToNumber(ud_rem_kileremme_antal) else ToNumber(0) endif],
			  [string: if isDefined(ud_rem_kileremme_type) then ud_rem_kileremme_type else "" endif],
			  [number: if isDefined(ud_rem_centerafstand) then ToNumber(ud_rem_centerafstand) else ToNumber(0) endif],
			  [number: if isDefined(ud_rem_justering) then ToNumber(ud_rem_justering) else ToNumber(0) endif],
			  [string: if isDefined(filter1_type) and filter1_type = "Andet" then filter1_type_andet else if isDefined(filter1_type) then filter1_type else "" endif],
			  [number: if isDefined(filter1_antal) then ToNumber(filter1_antal) else ToNumber(0) endif],
			  [number: if isDefined(filter1_nr) then ToNumber(filter1_nr) else ToNumber(0) endif],
			  [string: if isDefined(filter1_klasse) then filter1_klasse else "" endif],
			  [number: if isDefined(filter1_bredde) then ToNumber(filter1_bredde) else ToNumber(0) endif],
			  [number: if isDefined(filter1_hoejde) then ToNumber(filter1_hoejde) else ToNumber(0) endif],
			  [number: if isDefined(filter1_laengde) then ToNumber(filter1_laengde) else ToNumber(0) endif],
			  [number: if isDefined(filter1_pose_antal) then ToNumber(filter1_pose_antal) else ToNumber(0) endif],
			  [string: if isDefined(filter2_type) and filter2_type = "Andet" then filter2_type_andet else if isDefined(filter2_type) then filter2_type else "" endif],
			  [number: if isDefined(filter2_antal) then ToNumber(filter2_antal) else ToNumber(0) endif],
			  [number: if isDefined(filter2_nr) then ToNumber(filter2_nr) else ToNumber(0) endif],
			  [string: if isDefined(filter2_klasse) then filter2_klasse else "" endif],
			  [number: if isDefined(filter2_bredde) then ToNumber(filter2_bredde) else ToNumber(0) endif],
			  [number: if isDefined(filter2_hoejde) then ToNumber(filter2_hoejde) else ToNumber(0) endif],
			  [number: if isDefined(filter2_laengde) then ToNumber(filter2_laengde) else ToNumber(0) endif],
			  [number: if isDefined(filter2_pose_antal) then ToNumber(filter2_pose_antal) else ToNumber(0) endif],
			  [string: if isDefined(filter3_type) and filter3_type = "Andet" then filter3_type_andet else if isDefined(filter3_type) then filter3_type else "" endif],
			  [number: if isDefined(filter3_antal) then ToNumber(filter3_antal) else ToNumber(0) endif],
			  [number: if isDefined(filter3_nr) then ToNumber(filter3_nr) else ToNumber(0) endif],
			  [string: if isDefined(filter3_klasse) then filter3_klasse else "" endif],
			  [number: if isDefined(filter3_bredde) then ToNumber(filter3_bredde) else ToNumber(0) endif],
			  [number: if isDefined(filter3_hoejde) then ToNumber(filter3_hoejde) else ToNumber(0) endif],
			  [number: if isDefined(filter3_laengde) then ToNumber(filter3_laengde) else ToNumber(0) endif],
			  [number: if isDefined(filter3_pose_antal) then ToNumber(filter3_pose_antal) else ToNumber(0) endif],
			  [string: if isDefined(bemaerkning) then bemaerkning else "" endif],
			  [string: if isDefined(filter1_ud_type) and filter1_ud_type = "Andet" then filter1_ud_type_andet else if isDefined(filter1_ud_type) then filter1_ud_type else "" endif],
			  [number: if isDefined(filter1_ud_antal) then ToNumber(filter1_ud_antal) else ToNumber(0) endif],
			  [number: if isDefined(filter1_ud_nr) then ToNumber(filter1_ud_nr) else ToNumber(0) endif],
			  [string: if isDefined(filter1_ud_klasse) then filter1_ud_klasse else "" endif],
			  [number: if isDefined(filter1_ud_bredde) then ToNumber(filter1_ud_bredde) else ToNumber(0) endif],
			  [number: if isDefined(filter1_ud_hoejde) then ToNumber(filter1_ud_hoejde) else ToNumber(0) endif],
			  [number: if isDefined(filter1_ud_laengde) then ToNumber(filter1_ud_laengde) else ToNumber(0) endif],
			  [number: if isDefined(filter1_ud_pose_antal) then ToNumber(filter1_ud_pose_antal) else ToNumber(0) endif],
			  [string: if isDefined(filter2_ud_type) and filter2_ud_type = "Andet" then filter2_ud_type_andet else if isDefined(filter2_ud_type) then filter2_ud_type else "" endif],
			  [number: if isDefined(filter2_ud_antal) then ToNumber(filter2_ud_antal) else ToNumber(0) endif],
			  [number: if isDefined(filter2_ud_nr) then ToNumber(filter2_ud_nr) else ToNumber(0) endif],
			  [string: if isDefined(filter2_ud_klasse) then filter2_ud_klasse else "" endif],
			  [number: if isDefined(filter2_ud_bredde) then ToNumber(filter2_ud_bredde) else ToNumber(0) endif],
			  [number: if isDefined(filter2_ud_hoejde) then ToNumber(filter2_ud_hoejde) else ToNumber(0) endif],
			  [number: if isDefined(filter2_ud_laengde) then ToNumber(filter2_ud_laengde) else ToNumber(0) endif],
			  [number: if isDefined(filter2_ud_pose_antal) then ToNumber(filter2_ud_pose_antal) else ToNumber(0) endif],
			  [string: if isDefined(filter3_ud_type) and filter3_ud_type = "Andet" then filter3_ud_type_andet else if isDefined(filter3_ud_type) then filter3_ud_type else "" endif],
			  [number: if isDefined(filter3_ud_antal) then ToNumber(filter3_ud_antal) else ToNumber(0) endif],
			  [number: if isDefined(filter3_ud_nr) then ToNumber(filter3_ud_nr) else ToNumber(0) endif],
			  [string: if isDefined(filter3_ud_klasse) then filter3_ud_klasse else "" endif],
			  [number: if isDefined(filter3_ud_bredde) then ToNumber(filter3_ud_bredde) else ToNumber(0) endif],
			  [number: if isDefined(filter3_ud_hoejde) then ToNumber(filter3_ud_hoejde) else ToNumber(0) endif],
			  [number: if isDefined(filter3_ud_laengde) then ToNumber(filter3_ud_laengde) else ToNumber(0) endif],
			  [number: if isDefined(filter3_ud_pose_antal) then ToNumber(filter3_ud_pose_antal) else ToNumber(0) endif],
			  [string: if isDefined(bemaerkning_ud) then bemaerkning_ud else "" endif],
  			  [number: if isDefined(roegspjaeld_antal) then ToNumber(roegspjaeld_antal) else ToNumber(0) endif],
			  [string: if isDefined(roegspjaeld_foto) then roegspjaeld_foto else "" endif],
			  [string: if isDefined(foto1) then foto1 else "" endif],
			  [string: if isDefined(foto2) then foto2 else "" endif],
			  [string: if isDefined(foto3) then foto3 else "" endif],
			  [string: if isDefined(foto4) then foto4 else "" endif]
			)</sql>

        <sql command="updatedata">update formular_ventilation set
		wkb_geometry = [geometry: ToGeometry(wkt)],
		dato = localtimestamp,
		formular = [string: formular],
		ref = [string: sessionid],
		stednavn = [string: if isDefined(stednavn) then stednavn else "" endif],
		adresse = [string: if isDefined(address) then address else "" endif],
		aggregatplacering = [string: if isDefined(aggregatplacering) then aggregatplacering else "" endif],
		betjeningsomraade = [string: if isDefined(betjeningsomraade) then betjeningsomraade else "" endif],
		kontaktperson_navn = [string: if isDefined(kontaktperson_navn) then kontaktperson_navn else "" endif],
		kontaktperson_tlf = [string: if isDefined(kontaktperson_tlf) then kontaktperson_tlf else "" endif],
		sagsnr = [string: if isDefined(sagsnr) then sagsnr else "" endif],
		anlaeg_nr = [string: if isDefined(anlaeg_nr) then anlaeg_nr else "" endif],
		rev_dato = [string: if isDefined(rev_dato) then rev_dato else "" endif],
		rev_navn = [string: if isDefined(rev_navn) then rev_navn else "" endif],
		tegning_nr = [string: if isDefined(tegning_nr) then tegning_nr else "" endif],
		ventilationprincip = [string: if isDefined(ventilationprincip) then ventilationprincip else "" endif],
		vent_type = [string: if isDefined(vent_type) then vent_type else "" endif],
		vent_automatik = [string: if isDefined(vent_automatik) then vent_automatik else "" endif],
		vent_luftmaengde_proj = [number: if isDefined(vent_luftmaengde_proj) then ToNumber(vent_luftmaengde_proj) else ToNumber(0) endif],
		vent_luftmaengde_maalt = [number: if isDefined(vent_luftmaengde_maalt) then ToNumber(vent_luftmaengde_maalt) else ToNumber(0) endif],
		vent_statisk_tryk_proj = [number: if isDefined(vent_statisk_tryk_proj) then ToNumber(vent_statisk_tryk_proj) else ToNumber(0) endif],
		vent_statisk_tryk_maalt = [number: if isDefined(vent_statisk_tryk_maalt) then ToNumber(vent_statisk_tryk_maalt) else ToNumber(0) endif],
		vent_total_tryk_proj = [number: if isDefined(vent_total_tryk_proj) then ToNumber(vent_total_tryk_proj) else ToNumber(0) endif],
		vent_total_tryk_maalt = [number: if isDefined(vent_total_tryk_maalt) then ToNumber(vent_total_tryk_maalt) else ToNumber(0) endif],
		vent_omdrejning_proj = [number: if isDefined(vent_omdrejning_proj) then ToNumber(vent_omdrejning_proj) else ToNumber(0) endif],
		vent_omdrejning_maalt = [number: if isDefined(vent_omdrejning_maalt) then ToNumber(vent_omdrejning_maalt) else ToNumber(0) endif],
		vent_maks_omdr_proj = [number: if isDefined(vent_maks_omdr_proj) then ToNumber(vent_maks_omdr_proj) else ToNumber(0) endif],
		vent_maks_omdr_maalt = [number: if isDefined(vent_maks_omdr_maalt) then ToNumber(vent_maks_omdr_maalt) else ToNumber(0) endif],
		vent_sef_proj = [number: if isDefined(vent_sef_proj) then ToNumber(vent_sef_proj) else ToNumber(0) endif],
		vent_sef_maalt = [number: if isDefined(vent_sef_maalt) then ToNumber(vent_sef_maalt) else ToNumber(0) endif],
		motor_type = [string: if isDefined(motor_type) then motor_type else "" endif],
		motor_effekt_proj = [number: if isDefined(motor_effekt_proj) then ToNumber(motor_effekt_proj) else ToNumber(0) endif],
		motor_effekt_maalt = [number: if isDefined(motor_effekt_maalt) then ToNumber(motor_effekt_maalt) else ToNumber(0) endif],
		motor_spaending_proj = [number: if isDefined(motor_spaending_proj) then ToNumber(motor_spaending_proj) else ToNumber(0) endif],
		motor_spaending_maalt = [number: if isDefined(motor_spaending_maalt) then ToNumber(motor_spaending_maalt) else ToNumber(0) endif],
		motor_stroemforbrug_proj = [number: if isDefined(motor_stroemforbrug_proj) then ToNumber(motor_stroemforbrug_proj) else ToNumber(0) endif],
		motor_stroemforbrug_maalt = [number: if isDefined(motor_stroemforbrug_maalt) then ToNumber(motor_stroemforbrug_maalt) else ToNumber(0) endif],
		motor_omdrejning_proj = [number: if isDefined(motor_omdrejning_proj) then ToNumber(motor_omdrejning_proj) else ToNumber(0) endif],
		motor_omdrejning_maalt = [number: if isDefined(motor_omdrejning_maalt) then ToNumber(motor_omdrejning_maalt) else ToNumber(0) endif],
		motor_cos_proj = [number: if isDefined(motor_cos_proj) then ToNumber(motor_cos_proj) else ToNumber(0) endif],
		motor_cos_maalt = [number: if isDefined(motor_cos_maalt) then ToNumber(motor_cos_maalt) else ToNumber(0) endif],
		drivkraft = [string: if isDefined(drivkraft) then drivkraft else "" endif],
		rem_ventilator_remskive = [string: if isDefined(rem_ventilator_remskive) then rem_ventilator_remskive else "" endif],
		rem_ventilator_udbor = [string: if isDefined(rem_ventilator_udbor) then rem_ventilator_udbor else "" endif],
		rem_motor_remskive = [string: if isDefined(rem_motor_remskive) then rem_motor_remskive else "" endif],
		rem_motor_udbor = [string: if isDefined(rem_motor_udbor) then rem_motor_udbor else "" endif],
		rem_kileremme_antal = [number: if isDefined(rem_kileremme_antal) then ToNumber(rem_kileremme_antal) else ToNumber(0) endif],
		rem_kileremme_type = [string: if isDefined(rem_kileremme_type) then rem_kileremme_type else "" endif],
		rem_centerafstand = [number: if isDefined(rem_centerafstand) then ToNumber(rem_centerafstand) else ToNumber(0) endif],
		rem_justering = [number: if isDefined(rem_justering) then ToNumber(rem_justering) else ToNumber(0) endif],
		ud_ventilationprincip = [string: if isDefined(ud_ventilationprincip) then ud_ventilationprincip else "" endif],
		ud_vent_type = [string: if isDefined(ud_vent_type) then ud_vent_type else "" endif],
		ud_vent_automatik = [string: if isDefined(ud_vent_automatik) then ud_vent_automatik else "" endif],
		ud_vent_luftmaengde_proj = [number: if isDefined(ud_vent_luftmaengde_proj) then ToNumber(ud_vent_luftmaengde_proj) else ToNumber(0) endif],
		ud_vent_luftmaengde_maalt = [number: if isDefined(ud_vent_luftmaengde_maalt) then ToNumber(ud_vent_luftmaengde_maalt) else ToNumber(0) endif],
		ud_vent_statisk_tryk_proj = [number: if isDefined(ud_vent_statisk_tryk_proj) then ToNumber(ud_vent_statisk_tryk_proj) else ToNumber(0) endif],
		ud_vent_statisk_tryk_maalt = [number: if isDefined(ud_vent_statisk_tryk_maalt) then ToNumber(ud_vent_statisk_tryk_maalt) else ToNumber(0) endif],
		ud_vent_total_tryk_proj = [number: if isDefined(ud_vent_total_tryk_proj) then ToNumber(ud_vent_total_tryk_proj) else ToNumber(0) endif],
		ud_vent_total_tryk_maalt = [number: if isDefined(ud_vent_total_tryk_maalt) then ToNumber(ud_vent_total_tryk_maalt) else ToNumber(0) endif],
		ud_vent_omdrejning_proj = [number: if isDefined(ud_vent_omdrejning_proj) then ToNumber(ud_vent_omdrejning_proj) else ToNumber(0) endif],
		ud_vent_omdrejning_maalt = [number: if isDefined(ud_vent_omdrejning_maalt) then ToNumber(ud_vent_omdrejning_maalt) else ToNumber(0) endif],
		ud_vent_maks_omdr_proj = [number: if isDefined(ud_vent_maks_omdr_proj) then ToNumber(ud_vent_maks_omdr_proj) else ToNumber(0) endif],
		ud_vent_maks_omdr_maalt = [number: if isDefined(ud_vent_maks_omdr_maalt) then ToNumber(ud_vent_maks_omdr_maalt) else ToNumber(0) endif],
		ud_vent_sef_proj = [number: if isDefined(ud_vent_sef_proj) then ToNumber(ud_vent_sef_proj) else ToNumber(0) endif],
		ud_vent_sef_maalt = [number: if isDefined(ud_vent_sef_maalt) then ToNumber(ud_vent_sef_maalt) else ToNumber(0) endif],
		ud_motor_type = [string: if isDefined(ud_motor_type) then ud_motor_type else "" endif],
		ud_motor_effekt_proj = [number: if isDefined(ud_motor_effekt_proj) then ToNumber(ud_motor_effekt_proj) else ToNumber(0) endif],
		ud_motor_effekt_maalt = [number: if isDefined(ud_motor_effekt_maalt) then ToNumber(ud_motor_effekt_maalt) else ToNumber(0) endif],
		ud_motor_spaending_proj = [number: if isDefined(ud_motor_spaending_proj) then ToNumber(ud_motor_spaending_proj) else ToNumber(0) endif],
		ud_motor_spaending_maalt = [number: if isDefined(ud_motor_spaending_maalt) then ToNumber(ud_motor_spaending_maalt) else ToNumber(0) endif],
		ud_motor_stroemforbrug_proj = [number: if isDefined(ud_motor_stroemforbrug_proj) then ToNumber(ud_motor_stroemforbrug_proj) else ToNumber(0) endif],
		ud_motor_stroemforbrug_maalt = [number: if isDefined(ud_motor_stroemforbrug_maalt) then ToNumber(ud_motor_stroemforbrug_maalt) else ToNumber(0) endif],
		ud_motor_omdrejning_proj = [number: if isDefined(ud_motor_omdrejning_proj) then ToNumber(ud_motor_omdrejning_proj) else ToNumber(0) endif],
		ud_motor_omdrejning_maalt = [number: if isDefined(ud_motor_omdrejning_maalt) then ToNumber(ud_motor_omdrejning_maalt) else ToNumber(0) endif],
		ud_motor_cos_proj = [number: if isDefined(ud_motor_cos_proj) then ToNumber(ud_motor_cos_proj) else ToNumber(0) endif],
		ud_motor_cos_maalt = [number: if isDefined(ud_motor_cos_maalt) then ToNumber(ud_motor_cos_maalt) else ToNumber(0) endif],
		ud_drivkraft = [string: if isDefined(ud_drivkraft) then ud_drivkraft else "" endif],
		ud_rem_ventilator_remskive = [string: if isDefined(ud_rem_ventilator_remskive) then ud_rem_ventilator_remskive else "" endif],
		ud_rem_ventilator_udbor = [string: if isDefined(ud_rem_ventilator_udbor) then ud_rem_ventilator_udbor else "" endif],
		ud_rem_motor_remskive = [string: if isDefined(ud_rem_motor_remskive) then ud_rem_motor_remskive else "" endif],
		ud_rem_motor_udbor = [string: if isDefined(ud_rem_motor_udbor) then ud_rem_motor_udbor else "" endif],
		ud_rem_kileremme_antal = [number: if isDefined(ud_rem_kileremme_antal) then ToNumber(ud_rem_kileremme_antal) else ToNumber(0) endif],
		ud_rem_kileremme_type = [string: if isDefined(ud_rem_kileremme_type) then ud_rem_kileremme_type else "" endif],
		ud_rem_centerafstand = [number: if isDefined(ud_rem_centerafstand) then ToNumber(ud_rem_centerafstand) else ToNumber(0) endif],
		ud_rem_justering = [number: if isDefined(ud_rem_justering) then ToNumber(ud_rem_justering) else ToNumber(0) endif],
		filter1_type = [string: if isDefined(filter1_type) and filter1_type = "Andet" then filter1_type_andet else if isDefined(filter1_type) then filter1_type else "" endif],
		filter1_antal = [number: if isDefined(filter1_antal) then ToNumber(filter1_antal) else ToNumber(0) endif],
		filter1_nr = [number: if isDefined(filter1_nr) then ToNumber(filter1_nr) else ToNumber(0) endif],
		filter1_klasse = [string: if isDefined(filter1_klasse) then filter1_klasse else "" endif],
		filter1_bredde = [number: if isDefined(filter1_bredde) then ToNumber(filter1_bredde) else ToNumber(0) endif],
		filter1_hoejde = [number: if isDefined(filter1_hoejde) then ToNumber(filter1_hoejde) else ToNumber(0) endif],
		filter1_laengde = [number: if isDefined(filter1_laengde) then ToNumber(filter1_laengde) else ToNumber(0) endif],
		filter1_pose_antal = [number: if isDefined(filter1_pose_antal) then ToNumber(filter1_pose_antal) else ToNumber(0) endif],
		filter2_type = [string: if isDefined(filter2_type) and filter2_type = "Andet" then filter2_type_andet else if isDefined(filter2_type) then filter2_type else "" endif],
		filter2_antal = [number: if isDefined(filter2_antal) then ToNumber(filter2_antal) else ToNumber(0) endif],
		filter2_nr = [number: if isDefined(filter2_nr) then ToNumber(filter2_nr) else ToNumber(0) endif],
		filter2_klasse = [string: if isDefined(filter2_klasse) then filter2_klasse else "" endif],
		filter2_bredde = [number: if isDefined(filter2_bredde) then ToNumber(filter2_bredde) else ToNumber(0) endif],
		filter2_hoejde = [number: if isDefined(filter2_hoejde) then ToNumber(filter2_hoejde) else ToNumber(0) endif],
		filter2_laengde = [number: if isDefined(filter2_laengde) then ToNumber(filter2_laengde) else ToNumber(0) endif],
		filter2_pose_antal = [number: if isDefined(filter2_pose_antal) then ToNumber(filter2_pose_antal) else ToNumber(0) endif],
		filter3_type = [string: if isDefined(filter3_type) and filter3_type = "Andet" then filter3_type_andet else if isDefined(filter3_type) then filter3_type else "" endif],
		filter3_antal = [number: if isDefined(filter3_antal) then ToNumber(filter3_antal) else ToNumber(0) endif],
		filter3_nr = [number: if isDefined(filter3_nr) then ToNumber(filter3_nr) else ToNumber(0) endif],
		filter3_klasse = [string: if isDefined(filter3_klasse) then filter3_klasse else "" endif],
		filter3_bredde = [number: if isDefined(filter3_bredde) then ToNumber(filter3_bredde) else ToNumber(0) endif],
		filter3_hoejde = [number: if isDefined(filter3_hoejde) then ToNumber(filter3_hoejde) else ToNumber(0) endif],
		filter3_laengde = [number: if isDefined(filter3_laengde) then ToNumber(filter3_laengde) else ToNumber(0) endif],
		filter3_pose_antal = [number: if isDefined(filter3_pose_antal) then ToNumber(filter3_pose_antal) else ToNumber(0) endif],
		bemaerkning = [string: if isDefined(bemaerkning) then bemaerkning else "" endif],
		filter1_ud_type = [string: if isDefined(filter1_ud_type) and filter1_ud_type = "Andet" then filter1_ud_type_andet else if isDefined(filter1_ud_type) then filter1_ud_type else "" endif],
		filter1_ud_antal = [number: if isDefined(filter1_ud_antal) then ToNumber(filter1_ud_antal) else ToNumber(0) endif],
		filter1_ud_nr = [number: if isDefined(filter1_ud_nr) then ToNumber(filter1_ud_nr) else ToNumber(0) endif],
		filter1_ud_klasse = [string: if isDefined(filter1_ud_klasse) then filter1_ud_klasse else "" endif],
		filter1_ud_bredde = [number: if isDefined(filter1_ud_bredde) then ToNumber(filter1_ud_bredde) else ToNumber(0) endif],
		filter1_ud_hoejde = [number: if isDefined(filter1_ud_hoejde) then ToNumber(filter1_ud_hoejde) else ToNumber(0) endif],
		filter1_ud_laengde = [number: if isDefined(filter1_ud_laengde) then ToNumber(filter1_ud_laengde) else ToNumber(0) endif],
		filter1_ud_pose_antal = [number: if isDefined(filter1_ud_pose_antal) then ToNumber(filter1_ud_pose_antal) else ToNumber(0) endif],
		filter2_ud_type = [string: if isDefined(filter2_ud_type) and filter2_ud_type = "Andet" then filter2_ud_type_andet else if isDefined(filter2_ud_type) then filter2_ud_type else "" endif],
		filter2_ud_antal = [number: if isDefined(filter2_ud_antal) then ToNumber(filter2_ud_antal) else ToNumber(0) endif],
		filter2_ud_nr = [number: if isDefined(filter2_ud_nr) then ToNumber(filter2_ud_nr) else ToNumber(0) endif],
		filter2_ud_klasse = [string: if isDefined(filter2_ud_klasse) then filter2_ud_klasse else "" endif],
		filter2_ud_bredde = [number: if isDefined(filter2_ud_bredde) then ToNumber(filter2_ud_bredde) else ToNumber(0) endif],
		filter2_ud_hoejde = [number: if isDefined(filter2_ud_hoejde) then ToNumber(filter2_ud_hoejde) else ToNumber(0) endif],
		filter2_ud_laengde = [number: if isDefined(filter2_ud_laengde) then ToNumber(filter2_ud_laengde) else ToNumber(0) endif],
		filter2_ud_pose_antal = [number: if isDefined(filter2_ud_pose_antal) then ToNumber(filter2_ud_pose_antal) else ToNumber(0) endif],
		filter3_ud_type = [string: if isDefined(filter3_ud_type) and filter3_ud_type = "Andet" then filter3_ud_type_andet else if isDefined(filter3_ud_type) then filter3_ud_type else "" endif],
		filter3_ud_antal = [number: if isDefined(filter3_ud_antal) then ToNumber(filter3_ud_antal) else ToNumber(0) endif],
		filter3_ud_nr = [number: if isDefined(filter3_ud_nr) then ToNumber(filter3_ud_nr) else ToNumber(0) endif],
		filter3_ud_klasse = [string: if isDefined(filter3_ud_klasse) then filter3_ud_klasse else "" endif],
		filter3_ud_bredde = [number: if isDefined(filter3_ud_bredde) then ToNumber(filter3_ud_bredde) else ToNumber(0) endif],
		filter3_ud_hoejde = [number: if isDefined(filter3_ud_hoejde) then ToNumber(filter3_ud_hoejde) else ToNumber(0) endif],
		filter3_ud_laengde = [number: if isDefined(filter3_ud_laengde) then ToNumber(filter3_ud_laengde) else ToNumber(0) endif],
		filter3_ud_pose_antal = [number: if isDefined(filter3_ud_pose_antal) then ToNumber(filter3_ud_pose_antal) else ToNumber(0) endif],
		bemaerkning_ud = [string: if isDefined(bemaerkning_ud) then bemaerkning_ud else "" endif],
		roegspjaeld_antal = [number: if isDefined(roegspjaeld_antal) then ToNumber(roegspjaeld_antal) else ToNumber(0) endif],
		roegspjaeld_foto = [string: if isDefined(roegspjaeld_foto) then roegspjaeld_foto else "" endif],
		foto1 = [string: if isDefined(foto1) then foto1 else "" endif],
		foto2 = [string: if isDefined(foto2) then foto2 else "" endif],
		foto3 = [string: if isDefined(foto3) then foto3 else "" endif],
		foto4 = [string: if isDefined(foto4) then foto4 else "" endif]
			
		where ogc_fid = [Number:ToNumber(update_id)]</sql>

			<!-- <debug>true</debug> -->
        <table geometrycolumn="wkb_geometry" name="formular_ventilation" pkcolumn="ogc_fid"/>
    </datasource>
</datasources>
